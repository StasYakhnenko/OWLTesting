@model string
@using OwlTesting.ViewModels
@Html.ValidationSummary()
<form asp-action="DeleteUser" role="form">
    @Html.Partial("_ModalHeader", new ModalHeader { Heading = "Видалення користувача" })

    <div class="modal-body form-horizontal">
        Ви точно хочете видалити @Model?
    </div>
    @Html.Partial("_ModalFooter", new ModalFooter { SubmitButtonText = "Видалити" })
</form>

